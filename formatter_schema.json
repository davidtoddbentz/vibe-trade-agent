{
  "title": "FormattedQuestions",
  "description": "Structured output containing categorized questions.",
  "properties": {
    "multiple_choice": {
      "description": "List of multiple choice questions",
      "items": {
        "type": "object",
        "properties": {
          "question": {
            "description": "The question text",
            "type": "string"
          },
          "answers": {
            "description": "List of answer options as strings (minimum 2 required)",
            "items": {
              "type": "string"
            },
            "minItems": 2,
            "type": "array"
          }
        },
        "required": [
          "question",
          "answers"
        ]
      },
      "type": "array"
    },
    "free_form": {
      "description": "List of free-form response questions as strings",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [],
  "type": "object"
}
